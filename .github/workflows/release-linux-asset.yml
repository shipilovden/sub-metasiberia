name: Release Linux Asset

on:
  release:
    types:
      - published
      - prereleased
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.0.13)"
        required: true

permissions:
  contents: write

concurrency:
  group: release-linux-asset-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    env:
      GLARE_CORE_LIBS: ${{ github.workspace }}
      WINTER_DIR: ${{ github.workspace }}/winter
      GLARE_CORE_TRUNK_DIR: ${{ github.workspace }}/glare-core
      CYBERSPACE_OUTPUT: ${{ github.workspace }}/substrata_output_linux
    steps:
      - name: Resolve tag
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.release.tag_name }}"
          if [[ -z "${TAG}" ]]; then
            TAG="${{ github.event.inputs.tag }}"
          fi
          if [[ -z "${TAG}" ]]; then
            TAG="${GITHUB_REF_NAME}"
          fi
          if [[ -z "${TAG}" ]]; then
            echo "Failed to resolve release tag."
            exit 1
          fi
          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"

      - name: Checkout sub-metasiberia
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ steps.meta.outputs.tag }}

      - name: Checkout glare-core
        uses: actions/checkout@v4
        with:
          repository: shipilovden/glare-core
          path: glare-core
          fetch-depth: 1

      - name: Checkout winter
        uses: actions/checkout@v4
        with:
          repository: glaretechnologies/winter
          path: winter
          fetch-depth: 1

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ruby \
            cmake \
            git \
            curl \
            libsdl2-dev \
            llvm-15 \
            llvm-15-dev \
            ninja-build \
            tar \
            xz-utils \
            libjpeg-dev \
            libssl-dev \
            libtls-dev \
            libglu1-mesa-dev \
            libx11-dev \
            libxrandr-dev \
            libxi-dev \
            libxinerama-dev \
            libxcursor-dev \
            libasound2-dev

      - name: Fetch Jolt and Luau sources
        run: |
          cd scripts
          ruby get_libs.rb

      - name: Prepare LLVM path expected by Winter
        run: |
          LLVM_EXPECTED_BIN="${GITHUB_WORKSPACE}/llvm/llvm_15_0_7_dylib_install/bin"
          mkdir -p "${LLVM_EXPECTED_BIN}"

          if command -v llvm-config-15 >/dev/null 2>&1; then
            ln -sf "$(command -v llvm-config-15)" "${LLVM_EXPECTED_BIN}/llvm-config"
          elif command -v llvm-config >/dev/null 2>&1; then
            ln -sf "$(command -v llvm-config)" "${LLVM_EXPECTED_BIN}/llvm-config"
          else
            echo "llvm-config not found."
            exit 1
          fi

      - name: Build and prepare libjpeg-turbo 3.0.0
        run: |
          set -euo pipefail

          JPEG_DIR="${GITHUB_WORKSPACE}/libjpeg-turbo/libjpeg-turbo-3.0.0-install"
          JPEG_ROOT="${GITHUB_WORKSPACE}/libjpeg-turbo"
          JPEG_SRC="${JPEG_ROOT}/libjpeg-turbo-3.0.0"
          JPEG_BUILD="${JPEG_ROOT}/libjpeg-turbo-3.0.0-build"
          mkdir -p "${JPEG_ROOT}"

          if [[ ! -d "${JPEG_SRC}" ]]; then
            curl -L -o /tmp/libjpeg-turbo-3.0.0.tar.gz \
              https://github.com/libjpeg-turbo/libjpeg-turbo/archive/refs/tags/3.0.0.tar.gz
            tar -xzf /tmp/libjpeg-turbo-3.0.0.tar.gz -C "${JPEG_ROOT}"
          fi

          cmake -S "${JPEG_SRC}" -B "${JPEG_BUILD}" \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_SHARED=OFF \
            -DCMAKE_INSTALL_PREFIX="${JPEG_DIR}"
          cmake --build "${JPEG_BUILD}" -j 4
          cmake --install "${JPEG_BUILD}"

      - name: Prepare expected LibreSSL layout
        run: |
          set -euo pipefail

          LIBRESSL_DIR="${GITHUB_WORKSPACE}/LibreSSL/libressl-3.5.2-install"
          mkdir -p "${LIBRESSL_DIR}/include" "${LIBRESSL_DIR}/lib"

          ln -sfn /usr/include "${LIBRESSL_DIR}/include"

          if [[ -f /usr/lib/x86_64-linux-gnu/libtls.a ]]; then
            ln -sf /usr/lib/x86_64-linux-gnu/libtls.a "${LIBRESSL_DIR}/lib/libtls.a"
          else
            ln -sf /usr/lib/x86_64-linux-gnu/libtls.so "${LIBRESSL_DIR}/lib/libtls.a"
          fi

          if [[ -f /usr/lib/x86_64-linux-gnu/libssl.a ]]; then
            ln -sf /usr/lib/x86_64-linux-gnu/libssl.a "${LIBRESSL_DIR}/lib/libssl.a"
          else
            ln -sf /usr/lib/x86_64-linux-gnu/libssl.so "${LIBRESSL_DIR}/lib/libssl.a"
          fi

          if [[ -f /usr/lib/x86_64-linux-gnu/libcrypto.a ]]; then
            ln -sf /usr/lib/x86_64-linux-gnu/libcrypto.a "${LIBRESSL_DIR}/lib/libcrypto.a"
          else
            ln -sf /usr/lib/x86_64-linux-gnu/libcrypto.so "${LIBRESSL_DIR}/lib/libcrypto.a"
          fi

      - name: Configure Linux build (SDL, CEF off)
        run: |
          cmake -S . -B build_linux \
            -DCEF_SUPPORT=OFF \
            -DUSE_SDL=ON \
            -DSDL_BUILD_DIR=/usr \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo

      - name: Build gui_client
        run: |
          cmake --build build_linux --target gui_client -j 4

      - name: Create Linux package tarball
        id: pack
        shell: bash
        run: |
          set -euo pipefail

          TAG="${{ steps.meta.outputs.tag }}"
          PKG_DIR="${RUNNER_TEMP}/metasiberia-linux-${TAG}"
          mkdir -p "${PKG_DIR}/data"

          GUI_BIN="${CYBERSPACE_OUTPUT}/test_builds/gui_client"
          SERVER_BIN="${CYBERSPACE_OUTPUT}/test_builds/server"

          if [[ ! -f "${GUI_BIN}" ]]; then
            echo "gui_client not found at ${GUI_BIN}"
            exit 1
          fi

          cp "${GUI_BIN}" "${PKG_DIR}/gui_client"
          chmod +x "${PKG_DIR}/gui_client"

          if [[ -f "${SERVER_BIN}" ]]; then
            cp "${SERVER_BIN}" "${PKG_DIR}/server"
            chmod +x "${PKG_DIR}/server"
          fi

          cp -r resources "${PKG_DIR}/data"
          cp -r shaders "${PKG_DIR}/data"
          cp -r glare-core/opengl/shaders "${PKG_DIR}/data"
          cp -r glare-core/opengl/gl_data "${PKG_DIR}/data"
          cp docs/licence.txt "${PKG_DIR}/licence.txt"

          {
            echo '#!/usr/bin/env bash'
            echo 'set -euo pipefail'
            echo 'HERE="$(cd "$(dirname "$0")" && pwd)"'
            echo 'cd "${HERE}"'
            echo 'exec ./gui_client "$@"'
          } > "${PKG_DIR}/run.sh"
          chmod +x "${PKG_DIR}/run.sh"

          ASSET_NAME="MetasiberiaBeta-Linux-${TAG}.tar.gz"
          ASSET_PATH="${RUNNER_TEMP}/${ASSET_NAME}"
          tar -C "${RUNNER_TEMP}" -czf "${ASSET_PATH}" "metasiberia-linux-${TAG}"

          echo "asset_name=${ASSET_NAME}" >> "${GITHUB_OUTPUT}"
          echo "asset_path=${ASSET_PATH}" >> "${GITHUB_OUTPUT}"

      - name: Upload Linux package to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ steps.meta.outputs.tag }}" "${{ steps.pack.outputs.asset_path }}" \
            -R "${{ github.repository }}" \
            --clobber

      - name: Upload Linux package as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.asset_name }}
          path: ${{ steps.pack.outputs.asset_path }}
          if-no-files-found: error
